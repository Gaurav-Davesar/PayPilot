# Backend - use the direct connection string from Neon (with sslmode=require).
# Do not commit the real value; add it only to apps/server/.env.
DATABASE_URL=
OPENAI_API_KEY=
PORT=5000
NODE_ENV=development

# Frontend
VITE_API_BASE_URL=http://localhost:5000
